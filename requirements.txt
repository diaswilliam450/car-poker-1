# Bittensor 10.5.0 (matches the finney chain runtime; older 8.x fails to decode
# current chain state). 10.x doesn't auto-nest custom --neuron.*/--netuid/--blacklist.*
# config args, so poker44.utils.config._apply_cli_args re-applies them. A fresh venv
# install pulls cyscale (not scalecodec); if both end up present, run:
#   pip install --force-reinstall --no-deps cyscale
bittensor==10.5.0
pydantic>=2.6
requests>=2.31
python-dotenv>=1.0
numpy>=1.24,<2.1
scipy>=1.10
scikit-learn>=1.3
pandas>=2.0
joblib>=1.3
# model_v2 detector + retrain (LightGBM primary; XGBoost used by sibling variants)
lightgbm>=4.0
xgboost>=2.0
wandb>=0.19
